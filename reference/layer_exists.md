# Check if a layer exists in a geopackage

Check if a layer exists in a geopackage

## Usage

``` r
layer_exists(gpkg, name)
```

## Arguments

- gpkg:

  path to .gpkg

- name:

  layer name

## Value

logical
