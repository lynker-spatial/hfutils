% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hf_invariants.R
\name{hf_check_merge_invariants}
\alias{hf_check_merge_invariants}
\title{Global-merge (Stage 4) conservation + completeness invariants}
\usage{
hf_check_merge_invariants(
  merged,
  expected = NULL,
  area_tol = 0.005,
  stage = "merge",
  strict = FALSE
)
}
\arguments{
\item{merged}{named list with `flowpaths`, `divides`, `nexus` (sf/data.frame)
for the merged domain product.}

\item{expected}{optional list of input expectations summed over the per-VPU
inputs: `vpus` (character), `n_flowpaths`, `n_divides`, `area_sqkm`. When a
field is absent the corresponding check reports as info (skipped).}

\item{area_tol}{fractional tolerance for divide-area conservation (default 0.005).}

\item{stage}{message label (default "merge").}

\item{strict}{if TRUE, a failed check stops execution (else warns).}
}
\value{
invisibly `list(ok, stage, checks)`.
}
\description{
Complements the structural merge checks (id-uniqueness, prefixes, toid
resolution, divide FK, DAG) with the conservation/completeness class that
catches SILENT data loss or corruption when per-VPU fabrics are merged into a
global domain: a dropped/duplicated feature, lost catchment area, a missing
VPU, zeroed drainage area (the `add_measures` zero-fill class), mixed CRS, or
an untagged feature. Reusable across any domain merge.
}
