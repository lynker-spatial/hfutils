# Mapshaper simplification wrapper with robust system calls and validation

Mapshaper simplification wrapper with robust system calls and validation

## Usage

``` r
simplify_process(catchments, keep, sys, gb = 8, force = TRUE)
```
