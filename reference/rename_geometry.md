# Rename geometry column of sf object

Rename geometry column of sf object

## Usage

``` r
rename_geometry(g, name)
```

## Arguments

- g:

  sf object

- name:

  new geometry name

## Value

sf object with renamed geometry
