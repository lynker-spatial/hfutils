% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qml.R
\name{create_style_row}
\alias{create_style_row}
\title{Build one `layer_styles` row for a GeoPackage layer}
\usage{
create_style_row(gpkg_path, layer_name, style_name, style_qml)
}
\arguments{
\item{gpkg_path}{Path to the GeoPackage.}

\item{layer_name}{Layer the style applies to.}

\item{style_name}{Name recorded for the style.}

\item{style_qml}{QML contents, from [read_qml()].}
}
\value{
A one-row data frame, or `NULL` if `layer_name` has no geometry
  column in `gpkg_path`.
}
\description{
Assembles a single row in the schema QGIS expects in a GeoPackage's
`layer_styles` table. The geometry column is looked up from
`gpkg_geometry_columns`, so a layer without geometry (an attribute table such
as `network`) yields `NULL` rather than an error: QGIS styles apply to
spatial layers only.
}
\examples{
\dontrun{
row <- create_style_row("hydrofabric.gpkg", "nexus", "nexus_style",
  read_qml(system.file("qml", "nexus.qml", package = "hfutils")))
}
}
