# Convert MULITLINESTINGS to LINESTRINGS

Convert MULITLINESTINGS to LINESTRINGS

## Usage

``` r
flowpaths_to_linestrings(flowpaths)
```

## Arguments

- flowpaths:

  a flowpath \`sf\` object

## Value

a \`sf\` object
