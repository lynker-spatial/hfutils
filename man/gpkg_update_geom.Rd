% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpkg_metadata.R
\name{gpkg_update_geom}
\alias{gpkg_update_geom}
\title{In-place geometry update for specific rows of a GeoPackage layer}
\usage{
gpkg_update_geom(gpkg, layer, id_col, sf_changed)
}
\arguments{
\item{gpkg}{Path to the GeoPackage.}

\item{layer}{Target layer name.}

\item{id_col}{Id column used to match changed rows.}

\item{sf_changed}{An `sf` of the changed features (must carry `id_col`).}
}
\value{
Invisibly `NULL`.
}
\description{
Writes the changed features to a temporary layer in the same GeoPackage, swaps
the geometry column into `layer` via a keyed (indexed) SQL UPDATE, then drops
the temp layer. Layer triggers are dropped/restored around the write (see
[gpkg_update_col()]).
}
