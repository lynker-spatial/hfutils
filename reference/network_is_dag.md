# Test if flow network is acyclic

Test if flow network is acyclic

## Usage

``` r
network_is_dag(fl, ID = "id", toID = "toid")
```

## Arguments

- fl:

  sf object with flowlines

- ID:

  character column name

- toID:

  character column name

## Value

logical
