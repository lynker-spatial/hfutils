# NA

## What does this PR do?

Briefly describe the change and link any related issue
(e.g. `Closes #123`).

## Checklist

Code follows the package style (passes `lintr` / `styler`)

Added/updated tests (`devtools::test()` passes)

Added/updated documentation + examples (`devtools::document()` run)

`R CMD check` is clean (`devtools::check()`)

Added a `NEWS.md` entry if user-facing
